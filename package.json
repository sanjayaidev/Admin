{
  "name": "clientpm",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack client project management app — plain HTML/JS frontend + Node API, no build step.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.11.5"
  }
}
